def Check_Permutation(m,n):
    sm=sorted(m)
    sn=sorted(n)
    if(len(m)==len(s)):
        if(sm==sn):
            return True
    else:
        return False
    
m=input()
n=input()
print(Check_Permutation(m,n))